// Funções utilitárias para conexão com banco de dados
// Neste exemplo, estamos usando dados em memória

export const connectDatabase = () => {
    console.log('Conectado ao banco de dados (simulado)');
  };
  
  export const disconnectDatabase = () => {
    console.log('Desconectado do banco de dados (simulado)');
  };